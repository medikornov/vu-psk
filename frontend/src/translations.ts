const translationsJson = require('../public/en.json');

interface Translations {
    header: {
        logoText: string,
        flowers: string,
        uploadFlowers: string,
        aboutUs: string;
    };
}

// TODO
export default translationsJson;